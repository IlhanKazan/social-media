
# Crash reporting. Unset locally so the SDK stays disabled; set in the
# production build. The DSN is public by design — it only permits sending.
VITE_SENTRY_DSN=
# Ties an issue to the build that produced it, e.g. the short git SHA.
VITE_RELEASE=
