# ==========================================
# MicroBlog - Local Development Environment
# ==========================================
#
# RENDER PROD REQUIRED (set in Render dashboard, not committed):
#   SPRING_PROFILES_ACTIVE=prod
#   DATABASE_URL          (auto-injected from Render Postgres)
#   JWT_SECRET            (min 32 chars, random)
#   CLOUDINARY_CLOUD_NAME, CLOUDINARY_API_KEY, CLOUDINARY_API_SECRET
#   FRONTEND_ORIGIN       (e.g. https://social-client.onrender.com)
#   EMAIL_ENABLED=true, RESEND_API_KEY, EMAIL_FROM, EMAIL_FROM_NAME
#   OPENAI_API_KEY        (for moderation; or set MODERATION_ENABLED=false)
# ==========================================

# --- Database ---
# Used by local docker-compose. Defaults to the postgres service.
DATABASE_URL=jdbc:postgresql://localhost:5432/social
DB_USERNAME=postgres
DB_PASSWORD=postgres

# --- Security ---
# Must be at least 32 characters long.
JWT_SECRET=dev-secret-must-be-at-least-32-bytes-long-please-change

# --- Refresh-token cookie (HttpOnly) ---
# The refresh token is delivered as an HttpOnly cookie, never in the body.
# Prod default is SameSite=None + Secure because client and api are likely
# cross-site on Render (*.onrender.com is a public suffix). Set SameSite=Lax
# only if both are served from one registrable domain. Leave DOMAIN empty
# unless you host both under a shared parent domain.
AUTH_COOKIE_SAMESITE=None
AUTH_COOKIE_SECURE=true
AUTH_COOKIE_DOMAIN=

# --- Cloudinary (Image Uploads) ---
# Required for posting images and updating avatars/covers.
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# --- Email Infrastructure (Resend) - Phase 21 ---
# Set to true and provide API key to enable email sending in local dev.
# EMAIL_ENABLED=false
# RESEND_API_KEY=
# EMAIL_FROM=noreply@yourdomain.com
# EMAIL_FROM_NAME=SocialHan
# EMAIL_APP_NAME=SocialHan
# Public URL of the brand logo shown in the email header (host it on Cloudinary/static).
# EMAIL_LOGO_URL=

# --- AI Content Moderation (OpenAI) - Phase 25 ---
# Set to true to enable the moderation pipeline.
# MODERATION_ENABLED=false
# OPENAI_API_KEY=

# --- AI Bot Service (OpenAI) - Phase 27 ---
# Uses the same OPENAI_API_KEY as moderation. No separate key needed.
# BOT_ENABLED=false
# BOT_OPENAI_MODEL=gpt-5.4-mini-2026-03-17   (optional override)

# --- Client ---
# The origin where the frontend is hosted. Used for CORS.
FRONTEND_ORIGIN=http://localhost:5173
